This directory is reserved for future LVGL adapter conformance tests.

The first production-grade LVGL pass should add host-behavior tests that cover:

1. sorting interactions
2. projection refresh after row updates
3. pinned-region container splitting
4. saved-state restore